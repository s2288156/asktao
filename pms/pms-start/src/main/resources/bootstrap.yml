spring:
  application:
    name: pms
  cloud:
    nacos:
      config:
        server-addr: asktao.com:8848
        file-extension: yml
        namespace: dev
        extension-configs:
          - data-id: pms.yml
            group: pms
            refresh: true
          - data-id: mysql.yml
            group: base
            refresh: false
server:
  port: 18010
